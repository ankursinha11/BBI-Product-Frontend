export default function SchemaPage() {
  return (
    <div>
      <h1 className="text-white text-5xl">Schema Page</h1>
      <p className="text-white text-2xl">
        Add ability to add data, upload csv, download sample topright like
        FeedTable
      </p>
      <p className="text-white text-2xl">
        show something kind of like the airflow diagram if it is a direct map
        show arrow, if it is rename show as maybe dashed arrow
      </p>
      <p className="text-white text-2xl">
        Show only 1 feed at a time but be able to select what feed to
        display.(show as a plus and a drop down to see what one looks better)
      </p>
    </div>
  );
}
